{% extends "template.html.twig" %}

{% block head %}
    <h1>Article et commentaires</h1>
{% endblock %}

{% block body %}

    <p><a href="index.php">Retour à la liste des billets</a></p>

        <div class="news">

            <h3>
                {{ post.title|escape }}
                <em>le {{ post.creationDate|date("d/m/Y h:m") }}</em>
            </h3>

                <p>
                    {{ post.content }}
                </p>
        </div>

            <h2>Commentaires</h2>

    <form action="index.php?action=addComment&amp;id={{ post.id }}" method="post">

        <div>
            <label for="author">Auteur</label><br />
            <input type="text" id="author" name="author" />
        </div>

        <div>
            <label for="comment">Commentaire</label><br />
            <textarea id="comment" name="comment"></textarea>
        </div>
    <br>
        <div>
            <input class="btn btn-success" type="submit" />
        </div>

    </form>

    <div>
{% for comment in comments %}

    <p> {{ comment.author }} le {{ comment.commentDate|date("d/m/Y h:m") }}</p>

    <p>{{ comment.comment }}</p>
</div>
    <p><a class="btn btn-warning" href="index.php?action=signalEditComment&amp;id={{ post.id }}&amp;idComment={{ comment.id }}">signaler </a></p>


{% endfor %}

{% endblock %}
